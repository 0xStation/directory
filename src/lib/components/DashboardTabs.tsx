import {
  Tabs,
  TabsList,
  TabsTrigger,
  TabsContent,
} from "@/lib/components/ui/TabsHorizontal";

const DashboardTabs = () => {
  // we might not even need tabs if we have no logs or global settings?
};
