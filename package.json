{
  "name": "group-os",
  "version": "1.0.0",
  "description": "A batteries-included open-source repo for creating and managing token ecosystems.",
  "scripts": {
    "build": "lerna run build --stream",
    "dev": "lerna run dev",
    "start": "lerna run start",
    "start-web": "lerna run start --scope web",
    "start-ponder": "lerna run start --scope ponder"
  },
  "author": "Station",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.0.2"
  }
}
